# 论文格式问题修复方案

> 目标：系统性修复论文中的格式不规范问题，按章节逐一推进
> 工作流：本文档列出全部问题清单 + 修复策略 → 您确认后按章节执行

---

## 一、整体扫描结果（量化诊断）

### 1.1 加粗使用过度（最严重）

中文论文加粗使用比例**学术规范**通常控制在 3-8%。当前情况：

| 章节 | 加粗处数 | 加粗字符数 | 中文字符总数 | 加粗占比 | 评价 |
|---|---|---|---|---|---|
| chapter1 (绪论) | 32 | 573 | 2702 | **21.2%** | 严重过度 |
| chapter2 (相关工作) | 9 | 83 | 6115 | 1.4% | 偏少（但可接受）|
| chapter3 (数据合成框架) | 148 | 1734 | 9590 | **18.1%** | 严重过度 |
| chapter4 (GUI) | 171 | 1819 | 10079 | **18.0%** | 严重过度 |
| chapter5 (世界知识) | 173 | 1710 | 8042 | **21.3%** | 严重过度 |
| chapter6 (结论) | 39 | 576 | 2656 | **21.7%** | 严重过度 |
| **总计** | 572 处 | | | | |

**问题诊断**：

- 加粗用得太多导致"重点失焦"——读者看每一段都是高亮，反而看不出真正想强调的核心
- 大量"句中加粗"破坏了段落整体流畅度
- 学位论文评审更看重"客观陈述"，过度强调反而不专业

### 1.2 模型名字不一致

| 出现形式 | 次数 | 出现位置 |
|---|---|---|
| `Qwen3VL-Think` | 10 | chapter1/3/6/mythesis（推荐保留） |
| `Qwen3VL Think` | 2 | chapter3 (空格代替连字符，不一致) |
| `Qwen3VL` | 12 | 多处（裸名） |
| `qwen3vl` | 1 | chapter2 (全小写) |
| `Qwen2.5-VL` | 3 | chapter5 |
| `qwen2.5-VL` | 9 | chapter3/5（全小写不一致） |
| `qwen2.5-VL-7b` | 9 | 同上 |
| `qwen2vl` | 2 | chapter4 (应为 Qwen2-VL) |
| `Ovis2.5` | 14 | chapter5（OK） |
| `gpt4o` vs `GPT-4o` | 4+3 | 不一致 |

**修复目标统一为**：

- `Qwen3-VL-Think`（带连字符，正式名）→ 全文 12+10+2=24 处
- `Qwen2.5-VL-7B`（首字母大写、B 大写）→ 全文统一
- `Qwen2-VL`（修正 qwen2vl）
- `GPT-4o`（带连字符，统一）

### 1.3 英文术语大小写不一致

| 同一术语的不同写法 | 出现次数 |
|---|---|
| `Pipeline` vs `pipeline` | 32 vs 17 |
| `bbox` vs `BBox` | 48 vs 1 |
| `OCR` vs `ocr` | 39 vs 4 |
| `caption` vs `Caption` vs `CAPTION` | 65 vs 14 vs 1 |
| `domain` vs `Domain` | 45 vs 1 |

**修复策略**：句首/标题首字母大写，正文中用统一小写形式（除专有名词）。

### 1.4 摘要规范问题

当前情况：

- 中文摘要：639 字（学位论文规范一般 300-600 字，**略超**）
- 英文摘要：366 词（一般 200-400 词，OK）
- 5 处加粗（建议摘要内不加粗或最多 1-2 处加粗）
- 6 段（含小标题），可能不太符合"连贯成段"的传统摘要规范

**修复建议**：摘要保留小标题结构（学位论文也能接受），但**去掉所有 \textbf{}** 让小标题作为唯一的视觉锚点；或者改为单段连贯叙述。

### 1.5 流程图文字重叠（待视觉确认）

可能存在重叠的图：

- 图 1-1 overview.pdf
- 图 3-1 gui_pipeline.pdf
- 图 5-1 auto_evol_pipeline.pdf（chapter3.tex）
- chapter5.tex 的 wk_pipeline 等

**修复策略**：用 matplotlib/sips 重新渲染并检查；调整 box 高度/字号/换行。

---

## 二、按章节修复优先级

### 🅰️ P0 高优先级（影响整体观感）

| 章节 | 主要问题 | 工作量 |
|---|---|---|
| **chapter1（绪论）** | 加粗 21.2%，模型名 4 处，研究背景段加粗过密 | 中 |
| **chapter3（数据合成框架）** | 加粗 18.1%，模型名 4 处，最长一章 | 大 |
| **chapter4（GUI）** | 加粗 18.0%，挑战表内 \allowbreak 过多 | 大 |
| **chapter5（世界知识）** | 加粗 21.3%，模型名 11 处不一致 | 大 |
| **chapter6（结论）** | 加粗 21.7%，与 chapter1 类似 | 中 |
| **mythesis.tex（摘要）** | 5 处加粗 + 段落数偏多 | 小 |

### 🅱️ P1 中优先级（细节统一）

- 英文术语大小写统一（Pipeline / bbox / OCR / caption 等）
- chapter4 表 3-1 的 `\allowbreak` 出现在表格里可能造成排版问题
- 全文 LaTeX 内引号 / 破折号 / 省略号风格统一

### 🅲 P2 可选（视觉优化）

- 流程图（overview / gui_pipeline / auto_evol_pipeline）字号、间距、文字重叠检查
- 表格列宽、跨页处理

---

## 三、修复策略：加粗精简（重点）

### 3.1 保留加粗的场景（约 5-8%）

- **每章每节最重要的核心术语首次定义**（如"数据能力光谱"首次出现时）
- **关键数字证据**（如 +20.6 pt、F1 0.324→0.846 这类数字）
- **Auto-Evol、感知端、推理端、综合中段**等核心概念术语首次出现
- **章节内部小标题或转折性强的关键词**（如 \paragraph 内的开头词）

### 3.2 删除加粗的场景（约 14-16%）

- 同一术语**多次重复加粗**（保留首次出现，后续去粗）
- "**强调性形容词**"（如"显著""明显""完整""全面"等）
- 描述性短语加粗（如"**数据源组织**、**结构化初始化**、**任务分解与生成**"这种成串加粗）
- 摘要中的 \textbf{} 全部移除（让段落小标题前缀如 "(1) 感知端" 自动加粗即可）

### 3.3 加粗精简执行规则

每章按以下 4 条规则筛除：

1. **首次出现保留，后续删除**：核心术语第一次以 \textbf{} 标出，第 2 次起去掉
2. **数字+核心结论保留**：「+20.6 pt」「Grounding F1 0.846」等保留
3. **罗列性加粗合并删除**：连续 3 个以上加粗短语并列时全部去粗
4. **形容词加粗一律删除**：「显著」「重要」「核心」「关键」加粗都去掉

预计每章可从 18-21% 加粗降到 6-8%，减少约 65% 的 \textbf{} 用量。

---

## 四、修复执行顺序（建议）

按"章节顺序 + 影响力"排：

1. **mythesis.tex 摘要**（先修，影响整体外观）
2. **chapter1 绪论**（影响第一印象）
3. **chapter6 结论**（与 chapter1 镜像，一起修能保持平衡）
4. **chapter3 数据合成框架**（核心章，加粗最多）
5. **chapter4 GUI**（次重，含表格 \allowbreak 修整）
6. **chapter5 世界知识**（最长，模型名最不一致）
7. **chapter2 相关工作**（小修即可）
8. **流程图重新渲染**（如果有视觉重叠）

每章修复后立即编译验证一次，避免出错累积。

---

## 五、单章修复模板（每章按以下 5 步走）

每章修复时执行以下 5 个动作：

### Step 1：加粗精简
- 扫描本章所有 \textbf{} 实例
- 按 §3.3 四条规则逐一筛除
- 目标：加粗占比降至 6-8%

### Step 2：模型名统一
- `Qwen3VL-Think` / `Qwen3VL` → `Qwen3-VL-Think` / `Qwen3-VL`
- `qwen2.5-VL-7b` → `Qwen2.5-VL-7B`
- `qwen2vl` → `Qwen2-VL`
- `gpt4o` → `GPT-4o`

### Step 3：英文术语大小写统一
- `pipeline` → `Pipeline`（论文核心概念）
- `BBox` → `bbox`
- `Caption` 在句中 → `caption`（除句首/标题）
- `Domain` → `domain`

### Step 4：清理 \allowbreak（仅 chapter3/4/6 需要）
- 检查表格内的 `\allowbreak` 是否影响显示
- 行内重复段如 `Calc/\allowbreak Impress/\allowbreak Writer` 保留
- 表格单元内的 `\allowbreak` 评估是否需要

### Step 5：编译验证
- `latexmk -xelatex` 重编译
- 检查页数、错误数
- 抽样查看本章 PDF 的视觉效果

---

## 六、关键决策点（请您确认）

1. **加粗目标占比**：目标 6-8% 是否同意？或更激进 3-5%？
2. **摘要小标题加粗**：保留 `(1) 感知端 ——` 这种段落前缀加粗，还是全部去粗？
3. **模型名规范**：采用 `Qwen3-VL-Think`（HuggingFace 官方写法带连字符）还是 `Qwen3VL-Think`（紧凑写法）？
4. **章节修复顺序**：按建议（摘要→1→6→3→4→5→2）还是按章节顺序（1→2→3→4→5→6）？
5. **是否同时清理 chapter2**：chapter2 加粗 1.4% 已经很低，是否略过？

---

## 七、修复后验收标准

修复完成后应满足：

- ✅ 全文加粗占比 ≤ 8%
- ✅ 模型名零不一致（grep 检查）
- ✅ 英文术语大小写统一
- ✅ 摘要 ≤ 600 中文字 / ≤ 400 英文词
- ✅ 编译零错误、零未定义引用
- ✅ Overfull \hbox 警告数量 ≤ 5

---

请审阅本方案，确认后我从 **mythesis.tex 摘要** 开始按章节逐一修复。如有方向上的调整也请告诉我。
