# GUI 感知端：数据合成全流程清晰说明

> 用于帮助理解和重组 PPT 第 8、9、10 页（GUI 部分）

## 一、问题：GUI 场景为什么特殊？

普通图文场景模型已经做得不错，但 GUI 场景有 5 个"硬约束"：

| # | 挑战 | 模型典型症状 |
|---|---|---|
| 1 | 控件密集、小目标多 | 小图标坐标偏移 30 像素，误点相邻控件 |
| 2 | 图片缩放导致信息损失 | 文字模糊、图标变形，OCR 失效 |
| 3 | 视觉 ↔ 语言绑定松散 | "用户名"标签和输入框无法对应 |
| 4 | 通用 vs 专用能力冲突 | 学了 GUI 就忘了通用对话；学了通用就丢了 GUI 定位 |
| 5 | 数据冗余与同质化 | 同款 App 反复采集，长尾控件覆盖不足 |

**核心需求**：模型要在 GUI 场景具备 **Referring（理解控件语义）** 与 **Grounding（精准定位控件）** 两项基础能力。

## 二、解决思路：6 项核心技术（C1–C6）

| # | 技术 | 维度 | 解决哪个挑战 |
|---|---|---|---|
| C1 | **三类训练任务联合监督** | 数据 | 打牢"语义+定位+状态"三条能力链路 |
| C2 | **23 类细粒度控件分类学** | 标注 | 降低类别歧义，原子粒度优先 |
| C3 | **控件有向关系图谱** | 标注 | 显式建模"标签-控件"等跨控件语义绑定 |
| C4 | **三元交互状态向量** | 标注 | 区分"看似可点"与"真可点" |
| C5 | **AnyRes + 1000 级坐标量化** | 架构 | 解决信息损失 + 跨分辨率泛化 |
| C6 | **DMT 双阶段混合微调** | 训练 | 缓解能力冲突与灾难性遗忘 |

**三段式逻辑**：

- **C1–C4 → 数据侧**：构建高密度结构化监督
- **C5 → 架构侧**：让模型能"看清"并"输出"高密度监督
- **C6 → 训练侧**：让模型在不丢通用能力的前提下"吸收"GUI 监督

## 三、数据合成 Pipeline（4 阶段闭环）

整个 GUI 数据合成是一条 4 阶段流水线，配合失败回流形成闭环：

### 阶段 1：结构化初始化（输入对齐）

- **数据来源**：开源（Rico/MobileViews/OS-Atlas）+ 闭源精标 v1/v2 + 合成/重写
- **统一表示**：
  - 截图统一格式（含分辨率、视图层级 VH）
  - bbox 坐标做 **1000 级量化**（映射到 0–999 整数区间，消除分辨率差异）
  - 动作空间统一（点击/输入/滑动等）

**作用**：把"图片"变成"可计算、可追溯"的中间表示，供后续标注和训练共用。

### 阶段 2：人机协同混合标注（监督密度提升）

这是 GUI 数据合成的**核心环节**，分两条线：

**自动预标注**（机器先做粗活）：

- YOLO 检测控件 bbox
- PaddleOCR 提取控件文字
- Qwen2VL 等多模态模型补全属性（类型、状态、图标含义）

**人工校验补充**（人类把关质量）：按照三件套标注规范：

- **23 类细粒度控件分类学**（C2）：BUTTON、TEXT_INPUT、SWITCH、NAVIGATION_BAR 等，剔除 CARD_VIEW/LIST_ITEM 等布局类
- **有向关系图谱**（C3）：
  - R_ext：标签→控件（"用户名"文本指向输入框）
  - R_val：值→控件（"张三"指向已填写的输入框）
  - R_func：功能依赖（关闭按钮 X 与广告弹窗）
- **三元状态向量**（C4）：
  - Activated（开关开启/Tab 选中）
  - Interactable（可交互 vs 置灰）
  - Filled（输入框是否已填写）

**作用**：从"只有 bbox + 类别"升级到"bbox + 类别 + 关系 + 状态"的高密度结构化监督。

### 阶段 3：三类 QA 合成（任务多样性）

基于上一阶段的高密度标注，生成 3 类训练任务（C1）：

| 任务 | 输入 | 输出 | 训练什么 |
|---|---|---|---|
| **Task Grounding** | 自然语言指令 + 截图 | 目标控件 bbox | 指令 → 定位 |
| **State Transition** | 两张连续操作截图 | 状态变化判断 | 跟踪界面状态转移 |
| **Referring（双向 QA）** | bbox + 截图 | 控件语义/属性/文本 | 定位 → 语义 |

**关键设计**：Referring 与 Grounding 在形式上**互逆**（指令↔目标控件描述），同页面双向构造，相互强化对齐稳定性。

### 阶段 4：质控 + 二级去重 + 失败回流

**质控**：bbox 合法性、OCR 一致性、指令-目标匹配、关系图完整性

**二级去重**：

- 一级（精确）：文件哈希/感知哈希直接消除重复帧
- 二级（近重复）：视觉 embedding + 页面指纹（OCR 文本集合 + 控件类型直方图 + 布局摘要）双阈值合并

**失败回流**：质控不过的样本回流到阶段 2 补标/重写/再采集，形成闭环。

## 四、训练策略（架构 + 训练）

数据合成完成后，进入训练阶段，两项关键设计：

### 1. AnyRes 高分辨率感知 + 文本化坐标（C5）

**视觉侧**（解决 GUI 截图细节丢失）：

- 全局缩放：保留页面整体布局视野
- 自适应网格切分：按长宽比切成多个高分辨率 Patch
- Patch 位置编码：避免"看清局部但不知在整屏哪里"
- 全局 + 局部特征融合：同时给出页面结构 + 控件细节

**语言侧**（解决跨分辨率泛化）：

- bbox 坐标统一离散化到 0–999 整数（1000 级量化）
- 使用特殊 Token：`<|box_start|>` / `<|box_end|>` 约束输出结构
- 让模型学到"相对位置"而非"绝对像素"

### 2. DMT 双阶段混合微调（C6）

**问题**：直接全量混合会触发"能力冲突"；纯顺序微调会"灾难性遗忘"

**DMT 策略**：

- 阶段 1（领域注入）：100% GUI 数据，重点微调语言模型，冻结视觉编码器大部分参数
- 阶段 2（通用恢复）：80–90% 通用多模态对话 + **15–20% GUI 高质量样本回放**

**效果**：在保持 GUI 定位精度的同时，恢复通用对话与推理能力。

## 五、实验结果（量化验证）

**评测设置**：Top 200 主流应用真实分布，控件级评测

### 数据维度消融（多源融合的有效性）

| 数据配置 | Grounding F1 | Referring F1 |
|---|---|---|
| Baseline | 0.324 | 0.523 |
| Baseline + 开源数据 | 0.557 | 0.612 |
| **Baseline + 开源 + 闭源** | **0.846** | **0.905** |

### 控件细粒度评测（节选）

| 控件类型 | IoU≥0.5 F1 |
|---|---|
| 按钮 | 0.947 |
| 输入框 | 0.946 |
| 开关 | 0.947 |
| 文本 | 0.862 |
| 图标 | 0.737 |
| 页面指示器 | 0.483（小目标仍是挑战）|

### Referring 属性预测

| 属性 | F1 |
|---|---|
| 类型 | 0.944 |
| 内部注释（控件内文本） | 0.915 |
| 图标含义 | 0.858 |
| 外部注释（标签-控件绑定） | 0.806 |
| 当前值（仅滑块/进度条有） | 0.634 |
| **加权平均** | **0.905** |

### 误差归因

两类主要剩余误差：

- 极密集区域的近邻混淆（图标间距过小）
- 文本-控件关系未充分建模时的语义歧义

## 六、PPT 第 8、9、10 页改写建议

当前 PPT 第 8 页内容杂乱，建议把 GUI 部分**从 3 张拆为 4 张**或**重新组织 3 张**：

### 方案 A：4 张幻灯片（推荐，结构最清晰）

| 新 # | 标题 | 内容 |
|---|---|---|
| 8 | **GUI 数据合成：三件套标注规范** | 23 类分类学 + 关系图谱 + 状态向量（核心创新点）|
| 9 | **GUI 数据合成 Pipeline 全景** | 现有图片不变 + 4 阶段一句话注释 |
| 10 | **GUI 训练策略：AnyRes + DMT** | AnyRes 视觉机制 + DMT 双阶段策略 |
| 11 | **GUI 实验结果** | 现有 3 张实验图 + 关键数字总结 |

### 方案 B：保持 3 张，但内容重组

| 新 # | 标题 | 内容 |
|---|---|---|
| 8 | **GUI 数据合成方法（4 阶段闭环）** | 4 阶段流程文字 + 三件套标注简介 |
| 9 | **GUI Pipeline 全景图** | 现有图片不变 |
| 10 | **GUI 训练策略与实验结果** | AnyRes + DMT 简介 + 关键数字 |

## 七、关键叙事建议

如果只能讲 5 分钟，按以下 3 句话讲：

1. **问题**：GUI 场景有 3 大硬约束（小目标 / 信息损失 / 视语松散），通用 MLLM 难以1. **问题**：GUI 场景有 3 大硬约束（小目标 / 信息损失 / 视语松散），通用 MLLM 难以稳定胜任 Referring 与 Grounding
2. **方法**：构建"4 阶段闭环 Pipeline + 三件套标注规范 + AnyRes + DMT"，从数据、架构、训练三方面系统应对
3. **结果**：Grounding F1 由 0.324 → 0.846，Referring F1 由 0.523 → 0.905，验证多源融合 + 结构化监督 + 双阶段微调的有效性

---

请告诉我您倾向方案 A（4 张）还是方案 B（3 张），我立即按选定方案重组 PPT 第 8-10 页内容。
